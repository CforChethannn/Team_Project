# Team_Project

git config --global user.name "Your Name"
git config --global user.email "Your Email"

git config --global core.editor "code --wait"
git config --global core.autocrlf "input"

<!-- to edit any globally saved -->
git config --global -e

